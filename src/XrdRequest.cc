
#include "XrdRequest.h"

using namespace XrdAdaptor;

/*
ClientRequest::getType()
{
    return m_type;
}

ClientReadRequest::getOffset()
{
    return m_offset;
}

ClientReadRequest::getBuffer()
{
    // TODO: exc
}

{
    if (m_type == READ_REQUEST)
    {
        fh->Read(m_offset, m_size, into, this);
    }
}

int
ClientRequest::getStatus()
{
}
*/

